# Dashboard release summary

This release adds a customer-facing local browser helper, preserves the protected candidate application/evidence feed, adds Cloudflare Pages SPA fallback behavior for direct dashboard URLs, and records the production host/auth findings discovered during the end-to-end audit.
