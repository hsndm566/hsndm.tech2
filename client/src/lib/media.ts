/**
 * Approved managed MP4 shared by the hero and See It Work explainer. Both experiences retain
 * robust dark fallbacks for reduced-motion preferences and autoplay-limited browsers.
 */
// GitHub Pages content is behind a Cloudflare cache on the public custom domain. Keep this
// immutable release key so newly deployed MP4s bypass a previously cached 404 safely.
const VIDEO_RELEASE_KEY = "d018ed4";

export const HERO_VIDEO_URL = `/manus-storage/gemini_generated_video_EA567831_5f93d04f.mp4?v=${VIDEO_RELEASE_KEY}`;
export const EXPLAINER_VIDEO_URL = `/manus-storage/gemini_generated_video_DCF37916_a9fca67a.mp4?v=${VIDEO_RELEASE_KEY}`;
